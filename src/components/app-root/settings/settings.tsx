import { Component } from '@stencil/core';

@Component({
  tag: 'ad-settings',
  styleUrl: 'settings.css',
  shadow: true,
})
export class AdSettings {
  render() {
    return (
      <div>Settings</div>
    )
  }
}
